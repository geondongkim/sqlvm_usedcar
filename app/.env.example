# app/.env.example
# 사용법:
#   cp .env.example .env
#   nano .env
#   chmod 600 .env

# SQL Server SA 비밀번호 (Step 4 설치 시 설정한 값)
SA_PASSWORD=ChangeMe@2026!

# DB 연결
DB_SERVER=localhost
DB_NAME=CarMarket
# DB_USER: VM SQL Server=sa, Azure SQL=sqladmin (기본값: sa)
DB_USER=sa

# Flask 포트
FLASK_PORT=5000
